# ================================================================
# Arabic Audio Understanding & Retrieval System v2
# requirements.txt — Fixed & Complete
# ================================================================

# ── Core Deep Learning ──────────────────────────────────────────
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.40.0
accelerate>=0.27.0
datasets>=2.18.0

# ── Speech Recognition ──────────────────────────────────────────
openai-whisper>=20231117
faster-whisper>=1.0.0          # Much faster CPU inference

# ── Audio Processing ─────────────────────────────────────────────
librosa>=0.10.1
soundfile>=0.12.1
ffmpeg-python>=0.2.0
pydub>=0.25.1                  # Audio chunking and format conversion

# ── YouTube Download ─────────────────────────────────────────────
yt-dlp>=2024.3.10              # YouTube + many other platforms

# ── NLP & Summarization ──────────────────────────────────────────
sentencepiece>=0.2.0
protobuf>=4.25.0
sacremoses>=0.0.53
nltk>=3.8.1

# ── Arabic NLP ───────────────────────────────────────────────────
pyarabic>=0.6.15               # Arabic normalization
arabic-reshaper>=3.0.0         # Arabic text display

# ── Embeddings & Search ──────────────────────────────────────────
sentence-transformers>=2.6.0
faiss-cpu>=1.8.0               # Use faiss-gpu if GPU available
numpy>=1.26.0
scipy>=1.12.0
scikit-learn>=1.4.0

# ── Evaluation ───────────────────────────────────────────────────
jiwer>=3.0.4                   # WER computation
rouge-score>=0.1.2
evaluate>=0.4.1
bert-score>=0.3.13             # BERTScore for semantic similarity

# ── Demo Interface ───────────────────────────────────────────────
gradio>=4.26.0
plotly>=5.20.0
pandas>=2.2.0

# ── Utilities ────────────────────────────────────────────────────
tqdm>=4.66.2
pyyaml>=6.0.1
python-dotenv>=1.0.1
requests>=2.31.0
huggingface-hub>=0.22.0
colorlog>=6.8.2

# ── Testing ──────────────────────────────────────────────────────
pytest>=8.1.0
pytest-cov>=5.0.0

# ── Optional Advanced ────────────────────────────────────────────
# Uncomment if needed:
# pyannote.audio>=3.2.0        # Speaker diarization
# speechbrain>=1.0.0           # Emotion detection
